Write a Prolog program to implement max(X, Y, M) so that M is the maximum of two numbers X
and Y.
Solution 
max(X, Y, M) :- X >= Y, M is X.
max(X, Y, M) :- X < Y, M is Y.
Output 
| ?- max(20,57,X).
X = 57
yes
| ?- max(9,5,X).
X = 9 ?
